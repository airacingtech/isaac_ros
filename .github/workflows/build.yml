name: build

# Compile-test ALL isaac_ros packages against the host stack this repo targets:
# CUDA 12.8 + TensorRT 10.15 (cuda12). Runs WITHOUT a GPU -- the NVENC/NVDEC codec
# dlopen()s the driver libs at runtime, so only headers are needed to compile.
#
# Guards the environment regressions we hit: CUDA 13 changed cuCtxCreate, and
# TensorRT 11 removed kFP16 / kEXPLICIT_BATCH -- both silently broke the build.
#
# Fast + small by design:
#   * The heavy, stable toolchain (CUDA/ROS/TRT/nv-codec-headers) is BAKED into a GHCR
#     image (docker/Dockerfile). It is rebuilt only when that Dockerfile changes and
#     lives in the container registry -- it never touches the 10 GB Actions cache.
#   * The only Actions cache is ccache (capped, zstd-compressed) -- a few hundred MB, not
#     the multi-GB build/install trees. Warm ccache makes recompiles near-instant.
#   * A paths filter skips runs that don't touch build-relevant files.

on:
  pull_request:
    paths: &build_paths
      - "**/*.c"
      - "**/*.cc"
      - "**/*.cpp"
      - "**/*.cu"
      - "**/*.cuh"
      - "**/*.h"
      - "**/*.hpp"
      - "**/*.cmake"
      - "**/CMakeLists.txt"
      - "**/package.xml"
      - "**/*.repos"
      - "docker/Dockerfile"
      - ".github/workflows/build.yml"
  push:
    branches: [art-jazzy]
    paths: *build_paths
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository }}/ci-deps

jobs:
  # Build the dependency image only if one for this exact Dockerfile doesn't already
  # exist in the registry. Tag = hash of the Dockerfile, so an unchanged Dockerfile is a
  # ~10s no-op and source-only PRs reuse the cached image.
  deps-image:
    runs-on: ubuntu-24.04
    outputs:
      ref: ${{ steps.tag.outputs.ref }}
    steps:
      - uses: actions/checkout@v4
      - id: tag
        run: echo "ref=$IMAGE:$(sha256sum docker/Dockerfile | cut -c1-12)" >> "$GITHUB_OUTPUT"
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build + push if missing
        run: |
          set -euxo pipefail
          REF="${{ steps.tag.outputs.ref }}"
          if docker manifest inspect "$REF" >/dev/null 2>&1; then
            echo "Dependency image $REF already exists -- reusing."
          else
            docker build -t "$REF" -f docker/Dockerfile docker
            docker push "$REF"
          fi

  compile:
    needs: deps-image
    runs-on: ubuntu-24.04
    container:
      image: ${{ needs.deps-image.outputs.ref }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    env:
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CCACHE_MAXSIZE: "2G"
      CCACHE_COMPRESS: "1"
    steps:
      - name: Checkout isaac_ros
        uses: actions/checkout@v4
        with:
          path: src/isaac_ros

      - name: Cache ccache (small; the only Actions cache)
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ccache-${{ runner.os }}-${{ github.sha }}
          restore-keys: ccache-${{ runner.os }}-

      - name: rosdep install (toolchain already baked; this pulls light ROS deps)
        shell: bash
        run: |
          set -euxo pipefail
          source /opt/ros/jazzy/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -y --rosdistro jazzy \
            --skip-keys "cuda cudart tensorrt libnvinfer libnvinfer-dev nvidia-encode nvidia-decode" || true

      - name: colcon build (all packages, ccache-accelerated)
        shell: bash
        run: |
          set -euxo pipefail
          source /opt/ros/jazzy/setup.bash
          export CUDA_HOME=/usr/local/cuda
          export PATH=/usr/local/cuda/bin:$PATH
          ccache -z || true
          # *_models_install packages only download NGC assets (not a compile test) -- skip.
          colcon build \
            --packages-skip-regex ".*_models_install" \
            --cmake-args -DCMAKE_BUILD_TYPE=Release \
              -DCMAKE_C_COMPILER_LAUNCHER=ccache \
              -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
              -DCMAKE_CUDA_COMPILER_LAUNCHER=ccache \
            --parallel-workers 2 \
            --event-handlers console_direct+
          ccache -s || true
